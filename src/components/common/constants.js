const PRIORITY = [1, 2, 3];
const PRIORITY_LEVELS = ['High', 'Medium', 'Low'];
const DEFECT_STAGES = ['New', 'Open', 'In Progress', 'In Retest', 'Closed'];

module.exports = { PRIORITY, PRIORITY_LEVELS, DEFECT_STAGES };
